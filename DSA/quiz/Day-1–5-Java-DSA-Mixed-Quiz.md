## Day 1–5 Java DSA Mixed Quiz

Reply like:

```text
1.A
2.B
3.C
...
25.A
```

### 1. HashSet is mainly useful when we need:

A. Fast duplicate/existence checking  
B. Sorted output  
C. Index storage  
D. Queue behavior

### 2. Optimized Contains Duplicate uses:

A. HashMap  
B. HashSet  
C. Stack  
D. Queue

### 3. `HashSet.add(value)` returns `false` when:

A. Value is already present  
B. Value is new  
C. Set is sorted  
D. Value is negative

### 4. Contains Duplicate optimized time complexity is:

A. O(1)  
B. O(n)  
C. O(n²)  
D. O(log n)

### 5. Contains Duplicate optimized space complexity is:

A. O(1)  
B. O(n)  
C. O(n²)  
D. O(log n)

### 6. Two Sum on an unsorted array usually uses:

A. HashMap  
B. HashSet  
C. Stack  
D. Two sorted pointers

### 7. In Two Sum, `needed` is:

A. `target - current`  
B. `current - target`  
C. `target + current`  
D. `index - target`

### 8. HashMap in Two Sum stores:

A. `number → index`  
B. `index → number`  
C. `number → target`  
D. `target → number`

### 9. Why check `seen.containsKey(needed)` before `seen.put(current, index)`?

A. To avoid using the same element twice  
B. To sort the map  
C. To remove duplicates  
D. To make time O(n²)

### 10. Optimized Two Sum complexity is:

A. Time O(n), Space O(n)  
B. Time O(n²), Space O(1)  
C. Time O(log n), Space O(1)  
D. Time O(1), Space O(n)

### 11. Valid Anagram means:

A. Same length only  
B. Same characters with same counts  
C. Same first character  
D. Same sorted indexes

### 12. First check in Valid Anagram should be:

A. Length  
B. Last character  
C. First character  
D. Middle character

### 13. For lowercase English letters, optimized anagram can use:

A. `int[26]`  
B. `int[1000]` always  
C. `Queue`  
D. `Stack`

### 14. Frequency map stores:

A. character → count  
B. index → character  
C. target → count  
D. boolean → index

### 15. General HashMap anagram complexity is:

A. Time O(n), Space O(k)  
B. Time O(n²), Space O(1)  
C. Time O(log n), Space O(n)  
D. Time O(1), Space O(1)

### 16. Valid Palindrome mainly uses:

A. Two pointers  
B. HashMap  
C. Queue  
D. Heap

### 17. Valid Palindrome ignores:

A. Spaces, symbols, and case  
B. Only vowels  
C. Only numbers  
D. First character only

### 18. Java method to check letter or digit is:

A. `Character.isLetterOrDigit(ch)`  
B. `ch.isDigit()`  
C. `String.isLetter(ch)`  
D. `Character.isOnlyLetter(ch)`

### 19. Optimized palindrome space complexity is:

A. O(1)  
B. O(n)  
C. O(n²)  
D. O(log n)

### 20. Empty string or only spaces in palindrome problem usually returns:

A. true  
B. false  
C. error  
D. null

### 21. Two Sum II works best when array is:

A. Unsorted  
B. Sorted  
C. Random  
D. Empty only

### 22. In sorted Two Sum, if sum is too small:

A. Move left forward  
B. Move right backward  
C. Return false  
D. Use HashMap

### 23. In sorted Two Sum, if sum is too big:

A. Move left forward  
B. Move right backward  
C. Sort again  
D. Return true

### 24. Sorted Two Sum optimized complexity is:

A. Time O(n), Space O(1)  
B. Time O(n), Space O(n)  
C. Time O(n²), Space O(1)  
D. Time O(log n), Space O(1)

### 25. 80/20 rule for sorted pair-sum problem:

A. Use two pointers  
B. Use frequency map  
C. Use stack  
D. Use recursion
